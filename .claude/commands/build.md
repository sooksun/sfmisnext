Build the SFMIS project for production. Run in the project root:

```bash
npm run build:all
```

This builds:
- Frontend: Angular production build (uses --openssl-legacy-provider)
- Backend: NestJS TypeScript compilation

Check for build errors and report any issues in Thai.
