Start the SFMIS development environment. Run the following in the project root:

```bash
npm run start:all
```

This starts:
- Frontend: Angular dev server on http://localhost:4200
- Backend: NestJS dev server on http://localhost:3000

If there are dependency issues, run `npm run install:all` first.
